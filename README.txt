Each html file is linked with its own css file, to edit anything on a particular page just
edit the particular css file thats linked to the html file. 

To change something that all of the pages have in common such as the navbar or the footer,
use the "Style.css" file.

